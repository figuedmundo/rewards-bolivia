{
  "name": "rewards-bolivia",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "api": "npm run --workspace=api",
    "web": "npm run --workspace=web",
    "worker": "npm run --workspace=@rewards-bolivia/worker",
    "build": "npm run build --workspaces",
    "test": "turbo run test",
    "generate:sdk": "npm run --workspace=api start & sleep 5 && npx @openapitools/openapi-generator-cli generate -i http://localhost:3001/api-json -g typescript-axios -o packages/sdk && kill $(lsof -t -i:3001)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
